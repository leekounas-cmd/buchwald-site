/**
 * Formspree form endpoint IDs.
 *
 * Get the IDs from your Formspree dashboard: https://formspree.io/forms
 * Each form is a separate endpoint with its own inbox + analytics.
 *
 * To split a form: dashboard → "+ New Form" → copy the ID from the form's URL
 * (the part after `/f/`). Replace the value below. No other code changes needed.
 */

// /whitening-free landing page
// Captures: first name, last name, email, phone, preferred day
export const FORMSPREE_WHITENING = "xpqyyjkl";

// Homepage Quick Book modal (hero secondary CTA)
// Captures: first name, phone, optional best time to call
export const FORMSPREE_QUICK_BOOK = "xvzlgzjn";
